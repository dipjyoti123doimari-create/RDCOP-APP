ASSETS FOLDER
=============
Put an optional logo image here named exactly:

    app_logo.png

If this file exists, the app will:
- show it at the top of the sidebar, and
- use it as the browser tab icon.

If the file is missing, the app still works fine and shows a default
icon instead. So adding a logo is completely optional.
